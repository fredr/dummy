apiVersion: v1
kind: Service
metadata:
  name: dummy-canary
  namespace: dummy-canary
  labels:
    {{- .Values.labels | toYaml | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: 3000
    name: http
    targetPort: http
  selector:
    {{- .Values.labels | toYaml | nindent 4 }}
